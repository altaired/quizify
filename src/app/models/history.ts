import { NumberValueAccessor } from "@angular/forms/src/directives";

export interface History {
  tracks: string[];
  categories: string[];
  pickers: string[];
  introduced: boolean;
  games: number;
}
